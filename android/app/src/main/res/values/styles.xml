<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- La plantilla trae @null y entre la pantalla de arranque y la carga
             del WebView se cuela un destello blanco. Con el fondo de la app no
             se nota el cambio. -->
        <item name="android:background">@color/fondoApp</item>
        <item name="android:windowBackground">@color/fondoApp</item>
        <item name="android:statusBarColor">@color/fondoApp</item>
        <item name="android:navigationBarColor">@color/fondoApp</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>